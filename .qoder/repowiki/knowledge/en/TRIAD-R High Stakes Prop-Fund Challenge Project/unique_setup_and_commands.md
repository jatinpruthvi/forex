Run the full test and validation surface from the repo root: `python -m pytest tests/ -v` (expect 171 pass), `python tools/aggressive_optimizer.py` (~3 min multi-pair M5 optimization producing `findings_aggressive_optimizer.md`), and the sweep/reclaim pipeline via `tick_signal_builder.py` → `replay_export.py build` → `triad_validation.py validate`, all driven by the frozen `validation/triad_v2_1_registry.json`.